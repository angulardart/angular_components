# See with https://github.com/google/mono_repo.dart for details on this file

self_validate: true
travis:
  stages:
    - analyze_and_format
    - unit_test
    - build

merge_stages:
  - analyze_and_format
